import{c as a,S as o,j as t,m as r}from"./chunks/framework.DvQr6YbT.js";const u=JSON.parse('{"title":"Stability Contract","description":"","frontmatter":{},"headers":[],"relativePath":"reference/stability.md","filePath":"reference/stability.md","lastUpdated":1783505267000}'),i={name:"reference/stability.md"};function s(n,e,c,d,l,h){return o(),t("div",null,[...e[0]||(e[0]=[r('<h1 id="stability-contract" tabindex="-1">Stability Contract <a class="header-anchor" href="#stability-contract" aria-label="Permalink to “Stability Contract”">​</a></h1><p>As of 1.0.0, the surfaces below are frozen under semver: <strong>breaking any of them requires a major version</strong>. Minor and patch updates may add to these surfaces but never change or remove existing meaning. Every promise on this page is pinned by contract tests in the repository (<code>tests/contract/</code>) — the freeze is enforced by CI, not by good intentions.</p><h2 id="what-a-1-x-update-may-do-to-you" tabindex="-1">What a 1.x update may do to you <a class="header-anchor" href="#what-a-1-x-update-may-do-to-you" aria-label="Permalink to “What a 1.x update may do to you”">​</a></h2><p>Add new commands, flags, tools, optional fields, and optional files. Improve retrieval scores. Fix bugs. Refresh skill content.</p><h2 id="what-a-1-x-update-may-never-do-to-you" tabindex="-1">What a 1.x update may never do to you <a class="header-anchor" href="#what-a-1-x-update-may-never-do-to-you" aria-label="Permalink to “What a 1.x update may never do to you”">​</a></h2><p>Remove or rename a command, flag, or tool; change the meaning of an existing file or field; change JSON output keys; break a workspace or bundle created by 1.0.0.</p><h2 id="frozen-surfaces" tabindex="-1">Frozen surfaces <a class="header-anchor" href="#frozen-surfaces" aria-label="Permalink to “Frozen surfaces”">​</a></h2><h3 id="workspace-format" tabindex="-1">Workspace format <a class="header-anchor" href="#workspace-format" aria-label="Permalink to “Workspace format”">​</a></h3><ul><li>The meaning and location of <code>KNOWLEDGE.md</code>, <code>SCHEMA.md</code>, <code>INDEX.base</code>, the four compiled directories (<code>entities/</code>, <code>concepts/</code>, <code>comparisons/</code>, <code>queries/</code>), <code>inbox/</code>, and <code>Library/&lt;bundle-id&gt;/</code>.</li><li><code>.knowlery/manifest.json</code> (existing fields) and <code>.knowlery/bundles.json</code> (schemaVersion 1). Any 1.x reads state written by 1.0.0, forever.</li><li>The page tier rules (compiled dirs are agent-tier; everything else is user-tier).</li></ul><h3 id="cli-surface" tabindex="-1">CLI surface <a class="header-anchor" href="#cli-surface" aria-label="Permalink to “CLI surface”">​</a></h3><ul><li>Every command and subcommand shipped in 1.0, their positional arities, and their flags.</li><li><code>--json</code> output shapes: existing keys never change; additive keys are minor.</li><li>Exit-code semantics: <code>0</code> success — <strong>including findings</strong> (an abstaining query, an empty report); <code>1</code> operational failure; <code>2</code> usage error.</li><li>The <code>--kb</code> / <code>--dir</code> resolution rules: <code>--kb</code> resolves through the registry, <code>--dir</code> works forever, passing both is an error, and the registry is never a prerequisite.</li></ul><h3 id="mcp-contracts" tabindex="-1">MCP contracts <a class="header-anchor" href="#mcp-contracts" aria-label="Permalink to “MCP contracts”">​</a></h3><ul><li>The eight tool names (<code>list_kbs</code>, <code>query</code>, <code>stale</code>, <code>health</code>, <code>list_bundles</code>, <code>init_kb</code>, <code>capture</code>, <code>sync</code>), their input schemas (required fields and types; new <em>optional</em> inputs are minor), and their <code>structuredContent</code> shapes (existing keys). The advertised schemas carry the frozen keys — what a client introspects via <code>tools/list</code> is the contract, and the server validates every result against it at runtime.</li><li>Findings-vs-errors semantics: abstention, <code>healthy: false</code>, and stale-heavy reports are successful results; tool errors are reserved for broken calls.</li><li>The nine prompt names, the <code>knowlery://{kb}/{+path}</code> resource scheme, and the readable-surface allowlist boundary.</li><li><code>knowlery mcp serve</code> flags and the auth contract (bearer token, 401 shape).</li></ul><h3 id="bundle-format-okf" tabindex="-1">Bundle format (OKF) <a class="header-anchor" href="#bundle-format-okf" aria-label="Permalink to “Bundle format (OKF)”">​</a></h3><ul><li><code>knowlery-bundle.json</code> schemaVersion 1 fields and the zip layout (<code>index.md</code>, <code>agent-index.json</code>, <code>_sources/</code>, the update log).</li><li>Install/update gate semantics: version-increase requirement and local-modification refusal.</li></ul><h3 id="kb-registry" tabindex="-1">KB registry <a class="header-anchor" href="#kb-registry" aria-label="Permalink to “KB registry”">​</a></h3><ul><li><code>registry.json</code> schemaVersion 1, the name grammar (<code>[a-z0-9][a-z0-9-_]</code>, max 64 chars), reserved names (<code>*</code>, <code>all</code>), and the corrupt-is-loud rule (a damaged registry is reported, never silently reset).</li></ul><h2 id="explicitly-not-frozen" tabindex="-1">Explicitly not frozen <a class="header-anchor" href="#explicitly-not-frozen" aria-label="Permalink to “Explicitly not frozen”">​</a></h2><p>Stated just as plainly, so absence of a promise is never mistaken for one:</p><ul><li><strong>Retrieval ranking internals.</strong> Scores, ordering among candidates, and the confidence-gate thresholds may improve in any release. The contract is the <em>shape</em> of results and the abstention verdict string (<code>no-confident-match</code>), not the numbers.</li><li><strong>Skill prose.</strong> Skill content evolves; skill <em>names</em> are contract.</li><li><strong>The inner keys of <code>health</code>&#39;s <code>config</code> object.</strong> Individual check fields may be added, renamed, or retired as the health checker evolves — freezing them would freeze the checks themselves. Its contract is <code>healthy</code> (boolean) plus the presence of a <code>config</code> object.</li><li><strong>Plugin UI</strong> (dashboard layout, modals, settings organization), <strong>docs</strong>, and <strong>eval thresholds</strong>.</li><li>Private state under <code>.knowlery/</code> not listed above (activity receipts, reports, freshness files).</li><li>TypeScript internals: <code>src/</code> is not a public API; importing from the package is unsupported.</li></ul><h2 id="deprecation-path" tabindex="-1">Deprecation path <a class="header-anchor" href="#deprecation-path" aria-label="Permalink to “Deprecation path”">​</a></h2><p>A surface can gain a successor (a new flag, a new tool) in a minor release; the old one keeps working until a major version removes it. Aliases are not breaks.</p>',22)])])}const p=a(i,[["render",s]]);export{u as __pageData,p as default};
