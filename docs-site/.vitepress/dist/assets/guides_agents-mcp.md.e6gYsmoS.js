import{c as s,S as i,j as t,m as a}from"./chunks/framework.DvQr6YbT.js";const k=JSON.parse('{"title":"Agents & MCP","description":"","frontmatter":{},"headers":[],"relativePath":"guides/agents-mcp.md","filePath":"guides/agents-mcp.md","lastUpdated":1783496955000}'),n={name:"guides/agents-mcp.md"};function o(l,e,r,d,h,c){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="agents-mcp" tabindex="-1">Agents &amp; MCP <a class="header-anchor" href="#agents-mcp" aria-label="Permalink to “Agents &amp; MCP”">​</a></h1><p>Any MCP-capable agent — Claude Desktop, Claude Code, Cursor, gemini-cli — can talk to your knowledge bases directly. <code>knowlery mcp</code> runs an MCP server over stdio: the agent starts it, discovers the tools, and your knowledge becomes ambient in every conversation, with no per-conversation setup.</p><p>The server addresses knowledge bases by their <strong>registry names</strong> — register each one first:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">knowlery</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kb</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> work</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/vaults/work-kb</span></span></code></pre></div><h2 id="client-setup" tabindex="-1">Client setup <a class="header-anchor" href="#client-setup" aria-label="Permalink to “Client setup”">​</a></h2><p>All clients use the same command: <code>knowlery mcp</code>. If <code>knowlery</code> isn&#39;t on the PATH the client uses, install globally (<code>npm install -g knowlery</code>) or use the absolute path shown by <code>which knowlery</code>.</p><h3 id="claude-code" tabindex="-1">Claude Code <a class="header-anchor" href="#claude-code" aria-label="Permalink to “Claude Code”">​</a></h3><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mcp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> knowlery</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> knowlery</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mcp</span></span></code></pre></div><h3 id="claude-desktop" tabindex="-1">Claude Desktop <a class="header-anchor" href="#claude-desktop" aria-label="Permalink to “Claude Desktop”">​</a></h3><p>Add to <code>claude_desktop_config.json</code> (Settings → Developer → Edit Config):</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;mcpServers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;knowlery&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;knowlery&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;args&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mcp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="cursor" tabindex="-1">Cursor <a class="header-anchor" href="#cursor" aria-label="Permalink to “Cursor”">​</a></h3><p>Add to <code>~/.cursor/mcp.json</code> (global) or <code>.cursor/mcp.json</code> (per-project):</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;mcpServers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;knowlery&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;knowlery&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;args&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mcp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="gemini-cli" tabindex="-1">gemini-cli <a class="header-anchor" href="#gemini-cli" aria-label="Permalink to “gemini-cli”">​</a></h3><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gemini</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mcp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> knowlery</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> knowlery</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mcp</span></span></code></pre></div><p>Or add the same <code>mcpServers</code> block to <code>~/.gemini/settings.json</code>.</p><h2 id="tools" tabindex="-1">Tools <a class="header-anchor" href="#tools" aria-label="Permalink to “Tools”">​</a></h2><table tabindex="0"><thead><tr><th>Tool</th><th>Arguments</th><th>What it does</th></tr></thead><tbody><tr><td><code>list_kbs</code></td><td>—</td><td>Every registered KB with its path and live state</td></tr><tr><td><code>query</code></td><td><code>kb</code>, <code>question</code>, <code>k?</code></td><td>Deterministic retrieval; <code>kb: &quot;*&quot;</code> searches every KB with per-KB attribution</td></tr><tr><td><code>stale</code></td><td><code>kb</code></td><td>Compiled pages older than their sources, plus never-compiled notes</td></tr><tr><td><code>health</code></td><td><code>kb</code></td><td>Workspace integrity check</td></tr><tr><td><code>list_bundles</code></td><td><code>kb</code></td><td>Installed knowledge bundles with provenance</td></tr><tr><td><code>init_kb</code></td><td><code>name</code>, <code>path</code>, <code>platform?</code></td><td>Create and register a new KB — cold start from a conversation</td></tr><tr><td><code>register_kb</code></td><td><code>name</code>, <code>path</code></td><td>Bring an already-initialized KB into the registry (local stdio only)</td></tr><tr><td><code>capture</code></td><td><code>kb</code>, <code>content</code>, <code>title?</code></td><td>Save conversation content as a new note in the KB&#39;s <code>inbox/</code></td></tr><tr><td><code>sync</code></td><td><code>kb</code></td><td>Refresh built-in skills and instruction files to the installed version</td></tr></tbody></table><p>Every tool returns both readable text and structured JSON (the same shapes the CLI&#39;s <code>--json</code> flags have carried since 0.7).</p><p><strong>Findings are data.</strong> A query abstention (<code>verdict: &quot;no-confident-match&quot;</code>), an unhealthy <code>health</code> report, and a long <code>stale</code> list are all <em>successful</em> results — they are the answer, and a well-behaved agent relays them instead of retrying or guessing. Tool errors are reserved for actual breakage: unknown KB names, malformed input, unreadable disks.</p><ul><li><strong>Abstention is an answer.</strong> &quot;The knowledge base has no confident match&quot; is precisely what you want to hear when it&#39;s true.</li><li><strong><code>stale</code> output is a work list, not an alarm.</strong> It tells you what to re-cook when you next tend the vault.</li></ul><h2 id="the-write-path" tabindex="-1">The write path <a class="header-anchor" href="#the-write-path" aria-label="Permalink to “The write path”">​</a></h2><p>Exactly three tools write, and each is structurally bounded:</p><ul><li><strong><code>init_kb</code></strong> creates at most one new directory (its parent must already exist), refuses non-empty targets, and registers the result. A failed init cleans up after itself — a directory that existed before init is never deleted.</li><li><strong><code>register_kb</code></strong> brings an <em>existing, already-initialized</em> KB into the registry — it writes the registry file and nothing else, and a taken name is a hard error, not an auto-rename. It is not offered by <code>mcp serve</code>: the registry is machine-global state, so editing it stays a local act. Honest note: turning a folder of existing notes <em>into</em> a KB still needs the CLI (<code>knowlery init</code> works brownfield; MCP&#39;s <code>init_kb</code> deliberately refuses non-empty directories) — register it afterwards from the conversation.</li><li><strong><code>capture</code></strong> appends one new note to <code>inbox/</code> — filenames are constructed from a timestamp and a slug, never taken from the caller, and nothing is ever overwritten. Captures show up as <em>uncooked notes</em> in <code>stale</code> and are findable by <code>query</code> immediately; <code>/cook</code> treats <code>inbox/</code> as first-priority material. This is the &quot;remember this&quot; loop: capture in conversation, compile when you next cook.</li><li><strong><code>sync</code></strong> writes only content determined by the installed Knowlery version — the caller supplies nothing. The downgrade guard (workspace last synced by a newer version) is a tool error with the upgrade command in the message.</li></ul><p>Nothing writes the compiled layer (<code>entities/</code>, <code>concepts/</code>, <code>comparisons/</code>, <code>queries/</code>, <code>Library/</code>, <code>KNOWLEDGE.md</code>). Content is promoted there only through <code>/cook</code>&#39;s reviewed pipeline — the same gate philosophy as bundle export review.</p><p><strong>Write conduct</strong> (also stated in each tool&#39;s description): write tools act on the user&#39;s words, not the agent&#39;s initiative. Capture only what the user asked to save, and echo back the written path. Restate the resolved path <em>before</em> calling <code>init_kb</code> — creating a directory is the user&#39;s decision. Report sync&#39;s file list after running it.</p><h2 id="skills-as-prompts" tabindex="-1">Skills as prompts <a class="header-anchor" href="#skills-as-prompts" aria-label="Permalink to “Skills as prompts”">​</a></h2><p>The knowledge-workflow skills are exposed as MCP prompts, loadable from the client&#39;s prompt picker: <code>ask</code>, <code>cook</code>, <code>explore</code>, <code>challenge</code>, <code>ideas</code>, <code>audit</code>, <code>organize</code>, <code>vault-conventions</code>, <code>knowlery-cli</code>. Each returns the skill body verbatim — the same craft the Obsidian plugin installs, now machine-loadable anywhere.</p><h2 id="pages-as-resources" tabindex="-1">Pages as resources <a class="header-anchor" href="#pages-as-resources" aria-label="Permalink to “Pages as resources”">​</a></h2><p>Knowledge pages are readable at <code>knowlery://&lt;kb&gt;/&lt;path&gt;</code>, e.g. <code>knowlery://work/concepts/backpressure.md</code>. The resource list shows one entry point per KB (its <code>KNOWLEDGE.md</code>); agents reach specific pages through query results and wikilinks.</p><p>Only the <strong>curated knowledge surface</strong> is readable: <code>KNOWLEDGE.md</code>, the compiled directories (<code>entities/</code>, <code>concepts/</code>, <code>comparisons/</code>, <code>queries/</code>), and installed-bundle pages under <code>Library/</code>. Free-form notes (<code>Daily/</code>, <code>Projects/</code>, anything else) are refused — <code>query</code> may surface a raw note&#39;s title and path, but its content stays yours until you promote it with <code>/cook</code>.</p><h2 id="remote-access-self-hosted" tabindex="-1">Remote access (self-hosted) <a class="header-anchor" href="#remote-access-self-hosted" aria-label="Permalink to “Remote access (self-hosted)”">​</a></h2><p><code>knowlery mcp serve</code> runs the same server over Streamable HTTP — for reaching your KBs from another machine: a home server hosting them, a laptop away from home, a cloud agent given a tunnel URL.</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 1. Generate a token (Knowlery never generates or stores it for you)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">openssl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rand</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -hex</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 32</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.knowlery-mcp-token</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 2. Start the server — reads only, loopback bind</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">knowlery</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mcp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> serve</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --port</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8787</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --token-file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.knowlery-mcp-token</span></span></code></pre></div><p>The token can also come from the <code>KNOWLERY_MCP_TOKEN</code> environment variable (one source only — setting both is an error; it is never accepted as a bare CLI argument). Tokens under 16 bytes are refused. Every request must send <code>Authorization: Bearer &lt;token&gt;</code>; failures get a <code>401</code> that reveals nothing.</p><p><strong>Writes are off by default.</strong> Each write tool has its own flag — no bundling:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">knowlery</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mcp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> serve</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --port</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8787</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --token-file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.knowlery-mcp-token</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --allow-capture</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --allow-sync</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --allow-init</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --kb-root</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/kbs</span></span></code></pre></div><p>A write that isn&#39;t switched on is not present at all — it doesn&#39;t appear in <code>tools/list</code> and can&#39;t be called. <code>--allow-init</code> requires <code>--kb-root</code>: remote-initiated KBs may only be created under that directory.</p><p><strong>Keep the bind on <code>127.0.0.1</code> and put a tunnel in front</strong> — the server does no TLS; the tunnel owns the wire:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cloudflared</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tunnel</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --url</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://127.0.0.1:8787</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # quick tunnel</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tailscale</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> serve</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8787</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                             # tailnet-only</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -L</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 8787:127.0.0.1:8787</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my-server</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             # plain SSH</span></span></code></pre></div><p>Client config for a remote server (Cursor shown; Claude Code: <code>claude mcp add --transport http knowlery &lt;url&gt; --header &quot;Authorization: Bearer &lt;token&gt;&quot;</code>):</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;mcpServers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;knowlery-remote&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;url&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://your-tunnel-host/mcp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;headers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;Authorization&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bearer &lt;token&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="which-agents-can-reach-your-knowledge-honestly" tabindex="-1">Which agents can reach your knowledge, honestly <a class="header-anchor" href="#which-agents-can-reach-your-knowledge-honestly" aria-label="Permalink to “Which agents can reach your knowledge, honestly”">​</a></h3><table tabindex="0"><thead><tr><th>Agent class</th><th>1.0 answer</th></tr></thead><tbody><tr><td>Local MCP clients (Claude Desktop/Code, Cursor, gemini-cli)</td><td><code>knowlery mcp</code> over stdio — full support, all eight tools</td></tr><tr><td>Cloud agents with a shell (Cursor Cloud Agent, Codex-style)</td><td>already served: the CLI + bundle distribution</td></tr><tr><td>Web-only cloud agents (ChatGPT connectors, Gemini web, Claude web)</td><td>out of scope for 1.0 — self-hosted remote + a tunnel works for the determined; the zero-setup answer is a hosted platform, which is a recorded trajectory, not a 1.0 deliverable</td></tr></tbody></table><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to “Troubleshooting”">​</a></h2><ul><li><strong>&quot;Unknown knowledge base&quot;</strong> — the name isn&#39;t registered. <code>knowlery kb list</code> shows what is; <code>knowlery kb add &lt;name&gt; &lt;path&gt;</code> fixes it.</li><li><strong>Tools discovered but every call fails</strong> — check the KB states in <code>list_kbs</code>; <code>missing</code> means the folder moved, <code>uninitialized</code> means it was never set up (<code>knowlery init</code>).</li><li><strong>Client can&#39;t start the server</strong> — the client may not share your shell&#39;s PATH. Use the absolute path to <code>knowlery</code> in the config.</li></ul>`,47)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
