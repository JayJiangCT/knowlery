import{c as a,S as t,j as s,m as r}from"./chunks/framework.DvQr6YbT.js";const u=JSON.parse('{"title":"Design Decisions","description":"","frontmatter":{},"headers":[],"relativePath":"developer/design.md","filePath":"developer/design.md","lastUpdated":1783497907000}'),n={name:"developer/design.md"};function i(o,e,l,d,c,h){return t(),s("div",null,[...e[0]||(e[0]=[r('<h1 id="design-decisions" tabindex="-1">Design Decisions <a class="header-anchor" href="#design-decisions" aria-label="Permalink to “Design Decisions”">​</a></h1><p>The load-bearing decisions behind Knowlery, with the reasoning that produced them. Architecture says what the pieces are; this page says why they have the shape they do.</p><h2 id="determinism-over-prompting" tabindex="-1">Determinism over prompting <a class="header-anchor" href="#determinism-over-prompting" aria-label="Permalink to “Determinism over prompting”">​</a></h2><p>The earliest versions retrieved knowledge by instructing agents through prompts — a waterfall of &quot;first check X, then grep Y&quot;. It worked when the model felt like it. The 0.6 rewrite moved retrieval into code: a deterministic engine with measured quality, called <em>by</em> agents instead of performed by them.</p><p>The dividing rule that stuck: <strong>agents exercise judgment; code guarantees mechanics.</strong> Compiling knowledge (<code>/cook</code>) is judgment — it stays a skill. Finding candidates, detecting staleness, checking health, packaging bundles — those are mechanics, and every one of them moved into code with tests. When a behavior matters, the question is never &quot;how do we prompt this better&quot; but &quot;can this be a function&quot;.</p><h2 id="abstention-as-a-first-class-answer" tabindex="-1">Abstention as a first-class answer <a class="header-anchor" href="#abstention-as-a-first-class-answer" aria-label="Permalink to “Abstention as a first-class answer”">​</a></h2><p>A retrieval system that always returns <em>something</em> trains its callers to distrust it. The engine&#39;s confidence gate exists so that <code>no-confident-match</code> is a real, common verdict — the top candidate must clear specificity-weighted term coverage or source-graph evidence, not merely score highest.</p><p>This propagates outward as a product stance: the CLI exits 0 on abstention (it&#39;s a result, not an error), the MCP tool descriptions instruct agents to relay it rather than retry, and the eval harness holds unanswerable-question accuracy at 100% alongside recall floors — refusing to trade one for the other silently.</p><h2 id="findings-are-data-only-broken-calls-are-errors" tabindex="-1">Findings are data; only broken calls are errors <a class="header-anchor" href="#findings-are-data-only-broken-calls-are-errors" aria-label="Permalink to “Findings are data; only broken calls are errors”">​</a></h2><p>Every reporting surface distinguishes <em>the report contains bad news</em> from <em>the operation failed</em>. An unhealthy <code>health</code>, a long <code>stale</code> list, an abstaining <code>query</code> — all successful results carrying findings. Errors are reserved for calls that couldn&#39;t do what was asked: unknown KB names, malformed input, the sync downgrade guard (whose point is the write it refused to make).</p><p>The line matters most for agents: an agent that treats bad news as failure retries, and an agent that retries a truthful answer eventually hallucinates a better-sounding one.</p><h2 id="safety-arguments-must-be-structural" tabindex="-1">Safety arguments must be structural <a class="header-anchor" href="#safety-arguments-must-be-structural" aria-label="Permalink to “Safety arguments must be structural”">​</a></h2><p>Wherever Knowlery makes a safety promise, the preferred design makes the violation <em>impossible</em> rather than <em>checked for</em>:</p><ul><li><code>capture</code> has no path parameter — filenames are constructed from timestamp <ul><li>slug, so traversal cannot be expressed. The write uses the <code>wx</code> flag, so overwriting cannot happen at the syscall level. The inbox is <code>lstat</code>-verified to be a real directory, so a symlinked inbox cannot redirect writes.</li></ul></li><li>Remote access flags decide which tools are <em>registered</em> — a disallowed write is absent from <code>tools/list</code>, not present-but-refusing. There is no permission check to get wrong.</li><li><code>init_kb</code> canonicalizes before every check (parent realpath + candidate join), creates at most one leaf directory, and refuses non-empty targets — the blast radius is bounded by construction.</li><li>Bundle approval records content hashes, so editing an approved page automatically re-invalidates the approval.</li></ul><p>The pattern: when a review finds a boundary that is merely <em>filtered</em>, the fix is to redesign it so the bad state has no representation.</p><h2 id="review-gates-with-no-bulk-bypass" tabindex="-1">Review gates with no bulk bypass <a class="header-anchor" href="#review-gates-with-no-bulk-bypass" aria-label="Permalink to “Review gates with no bulk bypass”">​</a></h2><p>Nothing leaves a vault unreviewed: export requires an explicit approve/flag decision per item, and there is deliberately <strong>no approve-all flag</strong>. Where an agent mediates, conduct rules (written into the skills themselves) require presenting the full checklist verbatim and acting only on the user&#39;s stated decisions. Public publishing adds a second gate: re-acknowledging every risk item, because a public release is permanent.</p><p>The same philosophy gates the other direction — MCP write tools act on the user&#39;s words, never agent initiative — and the compiled knowledge layer is writable only through <code>/cook</code>&#39;s reviewed pipeline, on every transport.</p><h2 id="no-caches-no-indexes" tabindex="-1">No caches, no indexes <a class="header-anchor" href="#no-caches-no-indexes" aria-label="Permalink to “No caches, no indexes”">​</a></h2><p>Every query, staleness report, and health check is a live scan. This was measured before it was chosen: vault-scale corpora scan in milliseconds, so a cache would buy nothing and cost an invalidation protocol, a staleness bug class, and cross-shell coherence problems (three shells would share stale state). The decision has paid unexpected dividends: MCP statelessness and restart-safety fell out for free, and <code>kb add</code> needs no reindexing step.</p><h2 id="one-core-shells-as-transports" tabindex="-1">One core, shells as transports <a class="header-anchor" href="#one-core-shells-as-transports" aria-label="Permalink to “One core, shells as transports”">​</a></h2><p>Every feature lands in the platform-agnostic core first; shells only adapt I/O. This is enforced, not aspirational — the purity test fails any core module that imports Obsidian as a value, and the smoke test proves the built CLI artifact runs the full lifecycle with plain node.</p><p>The discipline compounds: the CLI (0.7) forced the <code>VaultFs</code> inversion; the MCP server (1.0) reused the inverted core untouched; the HTTP transport (1.0) reused the MCP handlers untouched. Each new shell has been cheaper than the last, and a future hosted platform is designed to be &quot;a second host, not a redesign&quot;.</p><h2 id="versions-are-contracts" tabindex="-1">Versions are contracts <a class="header-anchor" href="#versions-are-contracts" aria-label="Permalink to “Versions are contracts”">​</a></h2><ul><li>The manifest records which version last synced a workspace; an older shell refuses to sync rather than downgrade content a newer one wrote.</li><li>Bundle updates require version increases and refuse to overwrite local modifications (hash-detected) without <code>--force</code>.</li><li>Publishing is idempotent — republishing an existing version skips green instead of clobbering.</li><li>At 1.0, the workspace format, CLI flags/JSON shapes, and MCP tool contracts freeze under semver; renames after the freeze need an explicit decision, not a refactor.</li></ul><h2 id="credentials-are-never-knowlery-s" tabindex="-1">Credentials are never Knowlery&#39;s <a class="header-anchor" href="#credentials-are-never-knowlery-s" aria-label="Permalink to “Credentials are never Knowlery&#39;s”">​</a></h2><p>Knowlery verifies; it does not manage. Private bundle access delegates to the user&#39;s <code>gh</code> login. npm publishing uses OIDC trusted publishing instead of long-lived tokens. The MCP remote token is generated by the operator, read from env or file (never argv), compared in constant time, and stored nowhere. Every one of these chose &quot;integrate with existing credential ownership&quot; over &quot;hold a secret ourselves&quot;.</p><h2 id="measured-quality-frozen-floors" tabindex="-1">Measured quality, frozen floors <a class="header-anchor" href="#measured-quality-frozen-floors" aria-label="Permalink to “Measured quality, frozen floors”">​</a></h2><p>The eval harness is the reason retrieval changes are shippable: golden questions with expected pages, recall@10 and MRR computed per run, and a CI baseline that any regression fails against. When behavior intentionally changes, the floor is re-frozen in the same PR — quality moves are explicit, never drive-by. The same idea governs the abstention gate (unanswerable-set accuracy is part of the baseline) and, at 1.0, the contract freeze (schema assertions in tests are the floor for interfaces).</p><h2 id="spec-driven-development" tabindex="-1">Spec-driven development <a class="header-anchor" href="#spec-driven-development" aria-label="Permalink to “Spec-driven development”">​</a></h2><p>Every feature ships as: spec PR → maintainer review (often multiple rounds, findings fixed in the spec) → implementation → maintainer acceptance with real-world testing → status flip. The specs live in <code>specs/&lt;version&gt;/</code> and read as the project&#39;s decision log — including the findings that changed designs (the stateless-lifecycle contract, the symlinked-inbox refusal, the canonical-candidate algorithm all came from review rounds). Safety properties are written <em>as tests</em> in the spec before implementation begins.</p>',31)])])}const m=a(n,[["render",i]]);export{u as __pageData,m as default};
