# Knowlery 1.3.1 — Release Plan

**Theme:** attachments travel with knowledge — the sharing loop stops
shipping broken embeds. One feature, sequenced deliberately *before* 1.4:
multimodal cook intake will multiply attachment references in knowledge
pages, and the sharing side must not greet it with dead links.

## Features

| # | Feature | Spec | Depends on |
|---|---------|------|------------|
| F1 | Bundle attachments: embed discovery in the export closure, per-item review with sizes (images are the highest-leak-density artifacts), `_attachments/` emission through the Windows-portability machinery, a binary lane through zip/install, byte-hash integrity | [f1-bundle-attachments.md](./f1-bundle-attachments.md) | 1.2.7 portability map (reused), 0.8 review gate (extended) |
