# Nimbus Labs Knowledge Base (eval fixture)

This is a fixture vault for the Knowlery retrieval evaluation harness. It mimics the layout
created by the setup wizard: agent-maintained pages live in `entities/`, `concepts/`,
`comparisons/`, and `queries/`; everything else is user notes.

This file and `SCHEMA.md` are instruction files and are excluded from retrieval targets.
