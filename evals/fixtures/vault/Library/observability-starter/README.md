# Observability Starter Pack

A small fixture bundle in OKF v0.1 format, mimicking the layout produced by Knowlery's
bundle installer. Start at `index.md`; machine-readable entries live in
`agent-index.json`.
